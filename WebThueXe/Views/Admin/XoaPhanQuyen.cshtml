@model WebThueXe.Models.Quyen

@{
    ViewBag.Title = "XoaPhanQuyen";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>XoaPhanQuyen</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Quyen</h4>
    <hr />
    @Html.HiddenFor(model => model.maQuyen)

    <div class="container">
        <table class="table">
            <tbody>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.tenQuyen)</th>
                    <td>@Html.DisplayFor(model => model.tenQuyen, new { htmlAttributes = new { @class = "form-control" } })</td>
                </tr>
            </tbody>
        </table>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "QuanLyPhanQuyen")
        </div>
    }
</div>
